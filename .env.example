# Copy to .env.local and fill in. See /setup in the app for a walkthrough.

# Azure Speech resource (Keys and Endpoint page). Without these the app runs in demo mode.
AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=southeastasia

# One shared password for the whole app. Required when deployed online; optional locally.
APP_PASSWORD=
